```
source /etc/profile

sudo apt-get update

sudo apt install tree

tar -zxvf apache-maven-3.8.4-bin.tar.gz -C /home/package/
```

```
# 推送、拉取镜像
docker tag hello-world:latest cloud-linux.ip:registry.port/hello-world:latest
docker push cloud-linux.ip:registry.port/hello-world:latest
docker pull cloud-linux.ip:registry.port/hello-world:latest

# dockfile制作镜像
docker build -t ImageName:TagName dirName
docker build -t ImageName:TagName .

# docker-compose启动
docker-compose up -d
```

```
mvn -v
```






